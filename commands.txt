Run backend:
- cd /backend/
  - "go run cmd/app/main.go"


Run frontend:
- cd /frontend/
  - "npm run dev"
  - !non più utilizzabile (serve proxy) "npm run build" -> "npx serve build" (nginx-like)


Run Docker:
- "docker compose -f compose[.dev].yaml up"

Access DB in docker:
- "docker compose -f compose.dev.yaml exec db psql -U {postgres_user} -d {postgres_db}"

Access DB outside docker:
- "docker run --rm -it postgres:17 psql "host=host.docker.internal port=<port> user=<user> dbname=<db> sslmode=disable"

DB Migrations:
- "migrate -source file://G:/Lavoro/Web/Shokora/backend/cmd/migrate/migrations -database "postgres://devshokora:testdbdevpostgres@localhost:5434/postgresdev?sslmode=disable" up" -> usa goose invece di sta merda