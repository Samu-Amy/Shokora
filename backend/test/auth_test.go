package main

import "testing"

// TODO: crea App con config per i test (es. Sandbox mode per non inviare le email, test db, ecc.)

// TODO: fai test per quello che serve e poi scrivi il codice per implementare quelle cose

// TODO: testa anche che gli errori ritornari non siano interrors ma solo domerrors (anche se alcuni potrebbero non essere né interrors nè domerrors)

func RegisterUser(t *testing.T) {
	// t.Run("should not allow unauthenticated requests", func (t *testing.T) {

	// })
}

func LoginUser(t *testing.T) {
	// t.Run("should not allow unauthenticated requests", func (t *testing.T) {
	// TODO: test login user esistenti e user non esistenti (usando tutti i dati mescolati, in modo da usare email di utenti con password di altri)
	// })
}
