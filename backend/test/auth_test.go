package main

import "testing"

// TODO: fai test per quello che serve e poi scrivi il codice per implementare quelle cose

func RegisterUser(t *testing.T) {
	// t.Run("should not allow unauthenticated requests", func (t *testing.T) {

	// })
}
